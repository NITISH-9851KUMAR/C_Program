#include<stdio.h>
int main(){
    int x=5;
    printf("%d",x);
 // x++; //  x=x+1;
    printf("\n%d",++x);//x++means ,use x,then increment 
    //++x means ,increment then use
    //same condition in x-- and --x
    return 0;
}