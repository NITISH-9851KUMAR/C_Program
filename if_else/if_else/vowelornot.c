#include <stdio.h>
int mian()
{
    char  x, a, e, i, o, u;
    printf("Enter character :");
    scanf("%s", &x);
    // if (x == a ){
    //     printf("These are character ");
    // }
    // else
    //     printf("These are not character ");
    return 0;
}